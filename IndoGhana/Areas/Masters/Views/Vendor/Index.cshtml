
@{
    ViewBag.Title = "VendorList";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}



<script src="~/Scripts/jquery.dataTables.min.js"></script>
<link href="~/Content/jquery.dataTables.min.css" rel="stylesheet" />
<style>

</style>
<script>
    $(document).ready(function () {


        function DeleteRows() {
            debugger;
            var rowCount = tblsitelist.rows.length;
            for (var i = rowCount - 1; i > 0; i--) {
                Prodtbl.deleteRow(i);
            }
        
        }

        function loadVendorsitedata(celldata)
        {
            debugger;
          //  alert(celldata);
            var tbl = $('#tblsitelist').DataTable();
            if (tbl != null)
                tbl.destroy();
            $.ajax(
          {

              url: '@Url.Action("VendorSiteList")',
              data: { id: celldata },
              method: 'Get',
              dataType: 'json',

              success: function (data) {
                  debugger;
                  var datatableinstance = $('#tblsitelist').DataTable({

                      data: data,
                      'paging': true,
                      'sort': true,
                      'searching': true,
                      'scrolly': 200,
                      columns:
                      [


                          {
                              'data': 'VendorIDSiteID',
                              'searchable': true,
                              'sortable': true,
                              'render': function (VendorIDSiteID) {
                                  if (VendorIDSiteID != '') {
                                      //document.write(location.href+"<br/>");
                                      var urltoredirect = window.location.protocol + "//" + location.host + window.location.pathname + "/EditUser?User_Id=" + VendorIDSiteID;
                                      //document.write("urltoredirect=" + urltoredirect);
                                      return '<a href=' + '@Url.Action("EditVendorSiteDetails")' + '?id=' + VendorIDSiteID + '>' + VendorIDSiteID + '</a>'
                                  }
                              }
                          },
                           { 'data': 'SiteName' },
                            { 'data': 'SiteAddress' },
                           { 'data': 'ContactPersonName' },

                             { 'data': 'ContactNumber' },
                                { 'data': 'Email' },
                                   
                                      { 'data': 'CreatedBy' },
                                          {
                                              'data': 'CreationDate'
                              ,
                                              'searchable': true,
                                              'sortable': true,
                                              'render': function (CreationDate) {
                                                  if (CreationDate != '' && CreationDate != null) {
                                                      var date = new Date(parseInt(CreationDate.substr(6)));
                                                      var month = date.getMonth() + 1;
                                                      return date.getDate() + "/" + month + "/" + date.getFullYear();

                                                  }
                                                  else
                                                      return 'NA'
                                              }

                                          },

                         {
                             'data': 'Updatedby'
                              ,
                             'searchable': true,
                             'sortable': true,
                             'render': function (Updatedby) {
                                 if (Updatedby != '' && Updatedby != null) {
                                     var date = new Date(parseInt(Updatedby.substr(6)));
                                     var month = date.getMonth() + 1;
                                     return date.getDate() + "/" + month + "/" + date.getFullYear();

                                 }
                                 else
                                     return 'NA'
                             }

                         },
                         { 'data': 'UpdatedOn' },
                           { 'data': 'status' }




                      ]
                  });

            

              },
              error: function (err) {
                  debugger;
                  alert(err.responseText);
              }
          }

 )

        }
      
        function loaddataVendor()
        {

            $.ajax(
           {

               url: '@Url.Action("VendorList")',

               method: 'Get',
               dataType: 'json',

               success: function (data) {
                   debugger;
                   var datatableinstance = $('#Prodtbl').DataTable({

                       data: data,
                       'paging': true,
                       'sort': true,
                       'searching': true,
                       'scrolly': 200,
                       columns:
                       [
                           //{'data': 'VendorID'},

                           {
                               'data': 'VendorID',
                               'searchable': true,
                               'sortable': true,
                               'render': function (VendorID) {
                                   if (VendorID != '') {
                                       //document.write(location.href+"<br/>");
                                       var urltoredirect = window.location.protocol + "//" + location.host + window.location.pathname + "/EditUser?User_Id=" + VendorID;
                                       //document.write("urltoredirect=" + urltoredirect);
                                       return '<a href=' + '@Url.Action("EditVendorDetails")' + '?id=' + VendorID + '>' + VendorID + '</a>'
                                   }
                               }
                           },
                            { 'data': 'VendorName' },
                             { 'data': 'VendorAddress' },
                            { 'data': 'ContactPersonName' },

                              { 'data': 'ContactNumber' },
                                 { 'data': 'Email' },
                                   
                                       { 'data': 'CreatedBy' },
                                           {
                                               'data': 'CreationDate'
                               ,
                                               'searchable': true,
                                               'sortable': true,
                                               'render': function (CreationDate) {
                                                   if (CreationDate != '' && CreationDate != null) {
                                                       var date = new Date(parseInt(CreationDate.substr(6)));
                                                       var month = date.getMonth() + 1;
                                                       return date.getDate() + "/" + month + "/" + date.getFullYear();

                                                   }
                                                   else
                                                       return 'NA'
                                               }

                                           },

                          {
                              'data': 'Updatedby'
                               ,
                              'searchable': true,
                              'sortable': true,
                              'render': function (Updatedby) {
                                  if (Updatedby != '' && Updatedby != null) {
                                      var date = new Date(parseInt(Updatedby.substr(6)));
                                      var month = date.getMonth() + 1;
                                      return date.getDate() + "/" + month + "/" + date.getFullYear();

                                  }
                                  else
                                      return 'NA'
                              }

                          },
                          { 'data': 'UpdatedOn' },
                            { 'data': 'status' }




                       ]
                   });

                  // var table = $('#Prodtbl').DataTable();

                   //datatableinstance.on('select', function (e, dt, type, indexes) {
                   //    debugger;
                   //    if (type === 'row') {
                           
                   //        var data = table.rows(indexes).data().pluck('id');
                   //        alert(data);
                   //        // do something with the ID of the selected items
                   //    }
                   //});

                   //$('#Prodtbl tbody tr').click(function () {
                   //    var id = $(this).index();
                   //    //alert("row" + id);
                   //    var txt = $(this).closest('tr').children('td').text();
                   //    //alert(txt);
                   //    //var txt = $('#Prodtbl tbody tr td').closest("td").text();
                   //    //var title = $('#Prodtbl tbody tr td').eq($(this).index()).text();
                   //    //alert("cell data:" + title);
                   //    //var nTr = this.parentNode.parentNode;
                   //    // alert(nTr);
                   //    //DeleteRows();;
                   //    //loadVendorsitedata(celldata);

                   //});

                   $('#Prodtbl tbody td').click(function () {
                       //var id = $(this).index();
                       //alert("td=" + id);
                       //var txt = $(this).closest('td').text();
                       var id = $(this).index();
                       if ($(this).index() == 0) {
                           loadVendorsitedata($(this).closest('td').text());
                       }
                       //alert($(this).closest('td').text());

                       //var txt=$('#Prodtbl tbody tr td').closest("td").text();
                       //var title = $('#Prodtbl tbody tr td').eq($(this).index()).text();
                       //alert("cell data:" + title);
//                       var nTr = this.parentNode.parentNode;
                      // alert(nTr);
                       //DeleteRows();;
                       //loadVendorsitedata(celldata);

                   });

               },
               error: function (err) {
                   debugger;
                   alert(err.responseText);
               }
           }

  )
        }

        loaddataVendor();
    });

</script>

@using (Html.BeginForm())

{
    <div class="container" style="margin-top:50px;">
        <div class="row">
            <div class="col-md-10 col-lg-10  col-xs-12">
                <div class="col-md-5 col-lg-5 col-xs-6"><h4>Vendor List</h4></div>
                <div class="col-md-5 col-lg-5 col-xs-6"><h4> @Html.ActionLink("Add New Vendor", "CreateVendorDetails") </h4></div>

            </div>
        </div>

        <div class="row">
            <div class="col-md-10  col-lg-10  col-xs-12">
                <div class="table-responsive mycls">
                    <table id="Prodtbl" class="table table-bordered table-hover">
                        <thead>
                            <tr>
                                @*<th>VendorSiteID</th>*@
                                <th>VendorID</th>
                                <th>VendorName</th>
                                <th>VendorAddress</th>
                                <th>ContactPersonName</th>
                                <th>ContactNumber</th>
                                <th>Email</th>
                                
                                <th>CreatedBy</th>
                                <th>CreationDate</th>
                                
                                <th>Updatedby</th>
                                <th>UpdatedOn</th>
                                <th>status</th>
                            </tr>
                        </thead>
                        <tbody></tbody>
                    </table>
                </div>
            </div>
        </div>



        <div class="row">
            <div class="col-md-10  col-lg-10 col-xs-12">
                <div class="col-md-5 col-lg-5 col-xs-6"><h4>Vendor Branch List</h4></div>
                <div class="col-md-5 col-lg-5 col-xs-6"><h4> @Html.ActionLink("Add New Vendor Branch", "CreateVendorSiteDetails") </h4></div>

            </div>
        </div>

        <div class="row">
            <div class="col-md-10  col-lg-10  col-xs-12">
                <div class="table-responsive mycls">
                    <table id="tblsitelist" class="table table-bordered table-hover">
                        <thead>
                            <tr>
                                <th>VendorBranchID</th>
                                <th>SiteName</th>
                                <th>SiteAddress</th>
                                <th>ContactPersonName</th>
                                <th>ContactNumber</th>
                                <th>Email</th>
                                
                                <th>CreatedBy</th>
                                <th>CreationDate</th>

                                <th>Updatedby</th>
                                <th>UpdatedOn</th>
                                <th>status</th>
                            </tr>
                        </thead>
                        <tbody></tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
}


